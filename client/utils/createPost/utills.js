let formattingBtns = {
  bold: false,
};

function isBold() {
  formattingBtns.bold = !formattingBtns.bold;
}

function updateText(textareaElement) {
  if (formattingBtns.bold) {
  }
}

window.isBold = isBold;
window.updateText = updateText;

export { isBold, updateText };

console.logu;
